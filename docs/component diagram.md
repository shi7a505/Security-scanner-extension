![Architecture Diagram](.Security-scanner-extension/docs/component-diagram.png)

# 🧩 Browser Security Extension – Component Explanation

## 1️⃣ Extension Panel
ده الجزء اللي المستخدم بيتفاعل معاه مباشرة من المتصفح.

### **وظيفته**
- تشغيل أو إيقاف الإضافة (Enable/Disable)  
- بدء فحص الموقع يدويًا  
- عرض ملخص سريع للنتائج (Quick Summary)

### **أهميته**
- يعتبر **واجهة المستخدم المباشرة** (User Interface) للإضافة  
- يسمح للمستخدم بفهم الأكستينشن والتفاعل معه بسهولة

---

## 2️⃣ Page Scanner
ده الجزء اللي فعليًا بيعمل **scan للصفحات**.

### **وظيفته**
- جمع البيانات من صفحة الويب:  
  - DOM elements  
  - JavaScript sources  
  - Network requests  
- تحضير البيانات للـBackground Script  
- اكتشاف أي أنماط ممكن تشير لثغرات  

### **أهميته**
- يعتبر "العين" اللي بتشوف الصفحة  
- بيجمع كل المعلومات الضرورية للفحص قبل إرسالها للسيرفر

---

## 3️⃣ Background Script
ده الجزء اللي بيشتغل **في الخلفية** داخل المتصفح.

### **وظيفته**
- استقبال البيانات من Page Scanner  
- ترتيب وتجهيز البيانات للفحص  
- إرسال البيانات للـ**Backend API (/api/scan)**  
- إدارة إعدادات الأكستينشن (Settings)  
- تشغيل عمليات مستقلة بدون استهلاك كبير للمتصفح  

### **أهميته**
- الوسيط بين Page Scanner وBackend API (/api/scan)  
- بيخلي العملية سلسة وفعالة بدون أي تأثير على تجربة المستخدم

---

## 4️⃣ Backend API (/api/scan)
ده المخ الرئيسي اللي بيستقبل البيانات من الإضافة ويحللها قبل التخزين والعرض.

### **المكونات**
- **Rule Engine**  
  - مسؤول عن تحليل البيانات وتحديد الثغرات  
  - بيستخدم طرق زي Regex وSignatures وStatic checks  
- **Database / Storage**  
  - بيخزن كل نتائج الفحص لكل موقع  
  - البيانات المخزنة تشمل: نوع الثغرة، مستوى الخطورة، وقت الكشف، والتفاصيل الإضافية  
- **Dashboard Web App**  
  - يستعرض النتائج للمستخدم بشكل مرتب وسهل  
  - يعرض المواقع اللي تم فحصها، الثغرات، مستوى الخطورة، والإحصائيات

---

## 5️⃣ Flow Summary
1. المستخدم يفتح موقع → **Page Scanner** يبدأ جمع البيانات  
2. **Background Script** يستقبل البيانات → يرسلها للـ**Backend API (/api/scan)**  
3. **Rule Engine** يحلل البيانات → يحدد الثغرات  
4. النتائج تتخزن في **Database**  
5. **Dashboard Web App** تعرض النتائج للمستخدم

---

## 6️⃣ الخلاصة
- **Extension Panel** = واجهة المستخدم المباشرة للإضافة  
- **Page Scanner** = الجزء اللي بيعمل scan للصفحات  
- **Background Script** = الوسيط اللي بيبعت البيانات للBackend API (/api/scan)  
- **Backend API (/api/scan)** = المسؤول عن تحليل البيانات وتخزينها  
- **Dashboard Web App** = عرض النتائج بشكل كامل ومرتب
